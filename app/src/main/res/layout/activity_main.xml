<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">


    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginBottom="100dp"
        android:background="@android:color/darker_gray"
        android:padding="4dp"
        android:scrollbars="vertical" />

    <EditText

        android:layout_marginBottom="25dp"
        android:layout_marginStart="15dp"
        android:layout_alignParentLeft="true"
        android:layout_alignParentBottom="true"
        android:id="@+id/editTextInsert"
        android:layout_width="40dp"
        android:inputType="number"
        android:layout_height="wrap_content"
   />

    <Button
        android:layout_alignBottom="@+id/editTextInsert"
        android:id="@+id/btnInsert"
        android:layout_toEndOf="@+id/editTextInsert"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Insert" />

    <EditText

        android:id="@+id/editTextRemove"
        android:layout_width="40dp"
        android:layout_marginStart="65dp"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@+id/btnInsert"
        android:layout_toEndOf="@+id/btnInsert"
        android:inputType="number" />

    <Button
        android:id="@+id/btnRemove"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@+id/editTextRemove"
        android:layout_toEndOf="@+id/editTextRemove"
        android:text="Remove" />

</RelativeLayout>